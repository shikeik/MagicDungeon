ignored: indentFix codeFormatFix  

## 待办

优化ai提示词

增加1个指令来合批执行任务，一键更新，一键发布...

学习多子模块并运用

做个单独的任务管理app用于todo管理

这个类中使用全限定名的毛病，必须制裁他

修复增加Edit/Play模式后因为默认Edit所以所有EcsWorld演示场景不update的问题

- [ ] 引擎用户导出打包apk/exe还未实现
- [ ] 用户的jar的支持
- [ ] 加mainCamera组件
- [ ] gd项目开发者文档: 包括api手册和starter教程
  - 项目文档包括引擎内容总览, 和上面说的开发者文档, 以及版本(更新)文档
  - 这个也许可以用内置浏览器+github pages来托管, 做成md转html的方式来展示


- [ ] 版本历史的版本(tag)是逆序, 但是每个版本的commit是顺序的


- [ ] 增加logUI长按全量复制!

加mainCamera组件

gamemulmul的bug

文档适配为在线看

gd项目创建时 包名没有非法检测

changelog断点排查问题

先把模板搞了

textarea太蠢了

错了, 设计就错了, 用户根本不能导出或更新模板, 那不是模板, 模板只限于有限的几个内置, 用户导出的那叫项目, 只包含核心文件的项目, 这个项目用于用户间分享, 也可以上传到官方资源平台(这个后面再做)

DocServer要有main版方便单独用

点下组件X面板选中状态就错误取消了


一键打包apk/jar/exe新建并发布到一个tag

gdx升级: 1.12.1 -> 1.14.0

- [ ] 需要更健壮的tabbed面板, 然后完成自由嵌入分离tab操作功能
- [ ] region assetPath显示重设计
- [ ] 输一个字符就焦点被取消的bug(只有name输入会这样), 这可能不止是输字符会有bug, 还要检查其他是否有同类情况并修复
- [ ] javadoc 需要up-to-date
- [ ] FreePanViewer 字体缩放功能无效
- [ ] undo/redo 架构存在但没有实装，以及需要操作历史面板来可视化调试与跳转到指定操作节点

- [ ] 自定义类型绘制: FsmComp啥也不显示，需要对其持有实例Fsm fsm标记[SerializedField]并自定义该类型绘制方式(这里我们有index文件之后非常方便去查有自定义绘制注解的类), 不过这里引申一个问题，自定义绘制的父子类关系绘制继承问题
- [ ] 因为engine很多gradle任务资源需要保证同步, 所以要统一这块的log输出来便于调试验证是否每个流程正确执行了或是异常, 需要清晰的显示出来


- [ ] android异常捕获界面增加复制按钮
- [ ] 游戏内logUI文本增加长按复制功能
- [ ] RenderComp增加contains默认实现为sprite大小矩形, 无宽高则默认50x
- [ ] RunGame按钮需要继续跳转实机场景


#### 大项功能
- [ ] 未来架构 (Skeleton as GObjects)：您提到的“编辑器下是节点树，运行时烘焙为高性能数据流”是非常先进且标准的工业级方案（类似 Unity 的 Prefab 导入 或 Spine 的处理）。目前我们先保持现状，但设计渲染接口时要预留这种可能性。
- [ ] ui排版问题，整顿项目中所有ui面板与控件，统一排版逻辑，不要再重复手动实现ui了(而且手动实现还经常排版异常难看, 响应逻辑也问题颇多)
- - (已做优化了)阶段一要统一排版
- - 阶段二要做xml数据驱动布局
- - 阶段三后期可以实现可视化布局编辑器，就像android布局设计器一样


自动检测更新和云端模板包

一个实现起来比较模糊的功能：让脚本项目有使用引擎外外置jar的能力

项目内文字全中文化(考虑做i18n多语言)


LogErr得做了: 可以做一个解析格式化前缀文本为颜色字体的东西, 然后LogErr直接"<#FFFFFFFF>文字<#666666>文字2", 这里标准做法是什么我忘了, 写法是这样吗我也忘了
log应该能被复制

sourceJar得搞了(先得有注释才行, 给所有引擎方法打注释), 可以直接在ide跳转javadoc给脚本开发者写教程, 这必须得做起来

视口配置做成项目配置: 可配置横屏或竖屏, 以及长短边分辨率, 在Editor增加项目设置窗口来配置

吸纳开发者的好主意:
```
诶！我有个好主意
我把我的引擎做的游戏放taptap上
然后给个说明，这游戏是gd引擎做的，然后跳转qq群(这会同时吸引玩家与开发者, 所以得说明对引擎感兴趣的可加此群)按钮，以此吸纳玩家与开发者
不过要做到这步还需要最后的步骤，引擎得能把自己的运行时和脚本开发者的项目合在一起打包成apk和exe
```

Editor的New Script上下文菜单还在用硬编码脚本模板, 这里需要和创建项目的统一下逻辑, 新脚本文件模板也改为从assets/script_project_templates/NewScript.java获取的方式
脚本项目结构的规范化进阶
现在世界相机的视口大小和ui视口缩放绑定的, 按理应该基于基准视口大小而不是和ui视口耦合, 有空改








## 已搁置
做ui布局编辑器: 可调整控件和布局的对齐方式，调整布局位置...
- 使用xml做配置数据, 就像android可视化布局开发体验一样, 很舒服
