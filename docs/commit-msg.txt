feat(netcode): 大厅新增直连IP:Port模式，绕过自动网络类型识别

- SupabaseLobbyScreen 右侧面板新增"直连模式"区域
- 提供 ip:port 输入框（省略端口默认20001）+ 直连加入按钮
- directConnect() 方法直接调用 preConfigureAsClient，绕过
  回环/LAN/WAN 三级智能判断（同WiFi下存在误判为公网的问题）
- 支持纯IP或 ip:port 两种格式，含完善的输入校验与提示
