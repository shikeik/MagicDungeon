---
alwaysApply: false
description: 清理环节
---
# 清理 (Cleanup)

> **目标**: 结束当前任务状态，准备下一次循环。

1.  **环境清理**:
    - 移除临时 Log 和调试代码。
    - 删除临时文件。

2.  **状态更新**:
    - 在 `Tasks.md` 中标记当前任务为 `[x]`。
    - 移动已完成任务到下方 `Done` 区域。
    - 在 `Tasks.md` 顶部更新统计信息 (耗时、完成数)。

3.  **文档更新**:
    - 若有架构变动或踩坑经验，更新对应文档。
