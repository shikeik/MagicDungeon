# 骨骼编辑器完善任务清单

> **范式流程**:
> 1. 分析需求并完善具体需求以及扩展4的任务量, 罗列成为一个待办清单
> 2. 去做, 每做一步就回来勾选上做完的这条
> 3. 边做边勾选清单条目, 直到完成所有条目

## 任务列表

- [x] **重命名与基础设置**
    - [x] 将 `NeonHumanDemoScreen` 重命名为 `NeonSkelEditorScreen`
    - [x] 更新相关引用和类名

- [x] **1. 集成相机控制器**
    - [x] 在 `NeonSkelEditorScreen` 中实例化 `SimpleCameraController`
    - [x] 将相机控制器添加到输入多路复用器 (`InputMultiplexer`)
    - [x] 确保相机控制不与 UI 操作冲突

- [x] **2. 配置模式骨骼移动**
    - [x] 在 `SETUP` 模式下，允许通过鼠标拖拽移动骨骼位置
    - [x] 实现骨骼选中逻辑（点击选中，高亮显示）
    - [x] 确保拖拽时坐标转换正确（屏幕坐标 -> 世界坐标 -> 父骨骼局部坐标）

- [x] **3. 修正手肘反向**
    - [x] 检查 `lLegIK` 和 `rLegIK` 的 `bendPositive` 设置
    - [x] 调整 `bendPositive` 值以修正膝盖/手肘弯曲方向
    - [x] (如果手臂也有 IK) 检查手臂 IK 的弯曲方向

- [x] **4. 扩展功能 (参考蒙皮演示)**
    - [x] **UI 优化**: 使用 `VisUI` 优化控制面板，增加模式切换下拉框
    - [x] **骨骼树视图**: 添加 `VisTree` 显示骨骼层级结构
    - [x] **属性面板**: 选中骨骼后，显示其属性（名称、长度、位置、旋转）并允许修改
    - [x] **保存/加载**: (可选) 实现简单的 JSON 导出功能
    - [ ] **网格编辑**: (后续) 集成 `PolygonSpriteBatch` 的网格编辑功能 (参考 `PolyBatchTestScreen`)

- [x] **5. 验证与测试**
    - [x] 验证相机缩放和平移
    - [x] 验证配置模式下骨骼移动
    - [x] 验证动画模式下 IK 效果是否正确
