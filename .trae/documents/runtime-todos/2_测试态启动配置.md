# 测试态启动配置与自动测试任务清单

> **目标**: 优化开发测试流程，支持配置化直接启动目标场景，并自动执行模拟操作测试。

## 1. 启动配置架构 (Startup Configuration)
- [x] **创建调试配置类 `DebugLaunchConfig`**
    - [x] 定义 `boolean ENABLE_DIRECT_LAUNCH`: 是否启用直接启动
    - [x] 定义 `Class<? extends GScreen> TARGET_SCREEN`: 目标启动场景类
    - [x] 定义 `boolean ENABLE_AUTO_TEST`: 是否在启动后立即运行自动测试
- [x] **修改 `GdxLauncher` 入口逻辑**
    - [x] 在 `initGameContent()` 中接入 `DebugLaunchConfig`
    - [x] 如果启用了直接启动，跳过 `ExampleSelectScreen`，直接切换到 `TARGET_SCREEN`

## 2. 自动测试框架集成 (Auto-Test Integration)
- [x] **增强 `GScreen` 或 `AutoTestManager`**
    - [x] 确保场景加载完成后能触发测试脚本
    - [x] 检查 `AutoTestManager` 是否支持针对特定 Screen 的测试套件
- [x] **实现 `NeonSkelEditorScreen` 的自动测试脚本**
    - [x] **测试用例 1: 模式切换**
        - [x] 模拟点击 "Setup" 按钮
        - [x] 断言当前模式变为 `SETUP`
        - [x] 模拟点击 "Animate" 按钮
        - [x] 断言当前模式变为 `ANIMATE`
    - [x] **测试用例 2: 骨骼选中**
        - [x] 模拟点击树状图中的 "root" 节点
        - [x] 断言属性面板显示 "root" 信息
    - [x] **测试用例 3: 属性修改**
        - [x] 选中 "root" 后，模拟修改 X 坐标文本框
        - [x] 断言骨骼实际 X 坐标更新

## 3. 验证与文档
- [x] **验证直接启动流程**
    - [x] 配置启动 `NeonSkelEditorScreen`，验证是否直接进入
- [x] **验证自动测试流程**
    - [x] 启动后观察是否自动执行了点击和拖拽操作
- [x] **恢复默认配置**
    - [x] 确保提交代码时 `ENABLE_DIRECT_LAUNCH` 默认为 `false` (或提供 git ignore 的本地配置文件)
