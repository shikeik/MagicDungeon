我理解您的需求。现在的编辑器虽然架构升级了，但因为没有任何实体，看起来确实是空荡荡的。

为了解决这个问题，我将在 `EditorController` 中重新实现一个**可交互的测试场景**，恢复之前的演示体验。

### 计划实施步骤

1.  **构建测试场景 (`initTestScene`)**
    *   在 `EditorController` 中添加一个初始化方法。
    *   **创建主角实体**：使用项目现有的资源（如 `sprites/roles/enma/enma01.png`）创建一个带有 `TransformComponent` 和 `SpriteComponent` 的实体。
    *   **创建背景/参照物**：添加一个简单的背景图或参照物，让移动更明显。

2.  **恢复输入控制**
    *   **摇杆集成**：将之前代码中悬空未使用的 `joystick` 正式添加到 UI 界面中（通常放在左下角）。
    *   **逻辑连接**：在 `render` 循环中，读取摇杆的输入值，并实时更新主角实体的坐标，让角色能跑起来。

3.  **资源管理**
    *   使用 LibGDX 原生方式加载测试资源，确保在编辑器模式下稳定运行。

完成这些修改后，您打开编辑器将看到一个可以控制移动的角色，画面不再是空白的。