// 每次智能拾取1个任务, 根据智能优先级

- [x] (待检查) 权重gizmos: 权重显示不是渐变而是环形进度条式已明确权重进度0-1占比0和1的配色按spine来
- [x] (待检查) 权重刷模式披风我都看不见
- [x] (待检查) 刷子也看不见, 我按住拖动也没啥反应因为我都看不到刷子有多大啥形状
- [x] (待检查) 还有, 权重默认应该为1

每次进行非杂项的更新后需要发版, Patch或Minor, 这也是开发流程必要环节, 更新它

触发开发流程时: 智能依次拾取任务 -> 按开发流程完成任务 -> 标记Tasks.md该条为[x] (待检查) -> 拾取下一个 -> ..直至所有任务完成, 将此流程加入开发流程规则

开发流程的任务拆解规则更新: 在Task.md拾取并进行任务时如果任务过于繁重, 将复杂任务拆解为多个子任务, 如果还是复杂, 继续拆解, 直到任务复杂度能接受(不要过度拆解), 以及, 拆解需要直接编辑Tasks.md那个任务, 以此任务待办为准替代trae自身任务以达到长任务目的.

完成灵活滑动布局类开发与验证并逐步适配所有旧滑动布局: 现在的滑动布局交互不舒服, 我希望像windows这样, 鼠标只要悬浮到滑动布局overlap区域就可以直接自动切换焦点, 然后直接滚轮就滑动了, 而不是还需要点一下. 首先要完成这个自定义滑动布局封装, 然后创建一个测试场景以及其自动化测试来验证其效果, 需要有几个面板, 然后模拟鼠标悬浮移动到不同区域, 然后触发切换时断言焦点是否切换, 完成验证, 之后再挨个适配到所有旧滑动布局.

Tasks.md任务拾取规则更新: 你可以对Tasks.md中任务进行重排版, 因为通常用户排版都是乱的, 但是不要重写内容, 保持内容, 仅可重做排版划分

蒙皮场景: 
- 持久化改善: 现在json会保存class信息, 非常臃肿, 可以通过设计移除此信息来大幅缩减json大小
- 现在所有gizmos都太细太小, 加粗他们, 可以使用nenoBatch替代不方便的shapeRenderer
- 选中项Gizmos改为空心并加大size, 以解决遮挡权重gizmos问题
- 检查修复json读取报错问题
- 权重刷完善: 
  - 配色改为0灰不变, 1变为绿
  - 增加右键反向刷权重即减少权重功能


统一限制应用创建文件目录根为/<项目名>/, 并适配所有调用处, 以解决ide运行时Gdx.files.local()乱在根目录创建文件的问题
