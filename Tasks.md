// 每次智能拾取1个任务, 根据智能优先级

权重gizmos: 权重显示不是渐变而是环形进度条式已明确权重进度0-1占比0和1的配色按spine来

每次进行非杂项的更新后需要发版这也是开发流程必要环节, 更新它

触发开发流程时需要清空Tasks.md所有任务, 但是智能依次拾取任务, 创建一个循环, 拾取任务->按开发流程完成任务->标记Tasks.md该条完成->拾取下一个->..直至所有任务完成, 将此流程加入开发流程规则

权重刷模式披风我都看不见
刷子也看不见, 我按住拖动也没啥反应因为我都看不到刷子有多大啥形状, 
还有, 权重默认应该为1