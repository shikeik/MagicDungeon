<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
		  xmlns:tools="http://schemas.android.com/tools">

	<uses-feature android:glEsVersion="0x00020000" android:required="true"/>

	<!-- [新增] 读写权限 -->
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
	<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
	<!-- Android 11+ 需要此权限才能像电脑一样访问 SD 卡所有文件 (IDE 必备) -->
	<!-- 注意：上架 Google Play 需要特殊审核，但自己用没问题 -->
	<uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

	<!-- [新增] 网络权限 -->
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

	<!-- networkSecurityConfig="@xml/network_security_config 允许仅localhost和127.0.0.1的明文流量 -->
	<application
			android:allowBackup="true"
			android:fullBackupContent="true"
			android:icon="@drawable/ic_launcher"
			android:isGame="true"
			android:appCategory="game"
			android:label="@string/app_name"
			tools:ignore="UnusedAttribute"
			android:theme="@style/GdxTheme"
			android:networkSecurityConfig="@xml/network_security_config"
			android:requestLegacyExternalStorage="true"> <!-- 兼容旧版存储策略 -->

		<activity
				android:name=".AndroidGdxLauncher"
				android:label="@string/app_name"
				android:screenOrientation="landscape"
				android:configChanges="keyboard|keyboardHidden|navigation|orientation|screenSize|screenLayout"
				android:exported="true" tools:ignore="DiscouragedApi">
			<intent-filter>
				<action android:name="android.intent.action.MAIN"/>
				<category android:name="android.intent.category.LAUNCHER"/>
			</intent-filter>
		</activity>

		<activity
				android:name=".UncaughtExceptionActivity"
				android:screenOrientation="landscape"/>

		<!-- MT管理器 -->
		<activity
				android:name="bin.mt.file.content.MTDataFilesWakeUpActivity"
				android:exported="true"
				android:taskAffinity="${applicationId}.MTDataFilesWakeUp"
				android:excludeFromRecents="true"
				android:noHistory="true"/>
		<provider
				android:name="bin.mt.file.content.MTDataFilesProvider"
				android:permission="android.permission.MANAGE_DOCUMENTS"
				android:exported="true"
				android:authorities="${applicationId}.MTDataFilesProvider"
				android:grantUriPermissions="true">
			<intent-filter>
				<action
						android:name="android.content.action.DOCUMENTS_PROVIDER"/>
			</intent-filter>
		</provider>

		<provider
				android:name="androidx.core.content.FileProvider"
				android:authorities="${applicationId}.fileprovider"
				android:exported="false"
				android:grantUriPermissions="true">
			<meta-data
					android:name="android.support.FILE_PROVIDER_PATHS"
					android:resource="@xml/file_paths"/>
		</provider>

	</application>

</manifest>
